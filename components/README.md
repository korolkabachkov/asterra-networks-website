# components

Component inventory and specs — variants, states, and usage. Built on
[`../tokens/`](../tokens/); assembled in [`../design-system/`](../design-system/).

## Planned inventory

**Core:** Button (primary/secondary/ghost), Link, Input/Field, Select, Textarea,
Checkbox, Badge/Tag, Card, Tabs, Accordion, Breadcrumb, Pagination.

**Composite:** Header + nav (CTA: **Contact Us**), Footer, Hero (exploration),
Solution card, Industry card, Proof/stat block, Compliance/standards strip,
Resource/download card, Insight/article card, Inquiry form (**Submit a Request**),
Solution Finder (industry × need filter).

Each component documents: anatomy · variants · states (default/hover/focus/active/
disabled/error) · responsive behavior · a11y notes (WCAG 2.2 AA).

**Status:** _Not started._
